apiVersion: v1
kind: Service
metadata:
  name: session-db
  namespace: pattern-matcher-system
spec:
  type: ClusterIP
  selector:
    app: session-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
